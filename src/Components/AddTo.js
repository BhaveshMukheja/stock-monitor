import * as React from 'react';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';

export default function ColorButtons() {
  return (
    
      <Button className='absolute left-2' variant="outlined" color="error">
        Error
      </Button>
    
  );
}